version: '3.8'

services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    expose:
      - "5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/my-db
      - JWT_SECRET=mySuperSecretKey
    depends_on:
      - mongo

  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_BASE_URL=http://18.118.226.139:5000
    ports:
      - "3000:80"
    depends_on:
      - backend
