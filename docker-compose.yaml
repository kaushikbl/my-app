version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    expose:
      - "5000"

  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_BASE_URL=http://backend:5000
    ports:
      - "3000:80"
    depends_on:
      - backend
